[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "fiap-fase-4-aurora-siger"
version = "1.0.0"
requires-python = ">=3.13"
dependencies = [
    "rich>=13.0",
]

[project.optional-dependencies]
dev = []

[tool.hatch.build.targets.wheel]
packages = ["src"]
